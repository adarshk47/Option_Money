# ── Core ──────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
requests>=2.31.0
pytz>=2023.3

# ── Broker (Angel One SmartAPI) ───────────────────────
smartapi-python>=1.4.8
pyotp>=2.9.0
websocket-client>=1.6.0
logzero>=1.7.0

# ── Technical analysis ────────────────────────────────
# All indicators are implemented in pure pandas/numpy (ai_engine/indicators.py)
# so no TA library is required. Optional extras:
# pandas-ta>=0.4.67b0   # needs Python >= 3.12
# TA-Lib>=0.4.28        # needs the C library installed

# ── Machine learning ──────────────────────────────────
scikit-learn>=1.3.0
joblib>=1.3.0
# tensorflow>=2.15.0   # optional deep-learning support

# ── Web / API ─────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# ── Streamlit dashboard ───────────────────────────────
streamlit>=1.31.0
plotly>=5.18.0
streamlit-autorefresh>=1.0.1

# ── Alerts ────────────────────────────────────────────
python-telegram-bot>=20.0
pyttsx3>=2.90
plyer>=2.1.0

# ── Desktop build ─────────────────────────────────────
# pyinstaller>=6.0.0   # install only when building the .exe
